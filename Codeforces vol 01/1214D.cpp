#define ll long long int
#include<bits/stdc++.h>
#define maxn 1000003
using namespace std;

vector < char > G[maxn];
vector < ll > A[maxn];
ll n,m;
ll L[maxn] , D[maxn];
ll dx[]={0,1} , dy[] = {1,0};


bool dfs(ll x, ll y , ll px, ll qx)
{
    if ()
}

int main()
{
    cin  >> n >> m;
    for (int i=0;i<n;i++){
        string s;
        cin >> s;

        for (int j=0;j<m;j++)G[i].pb(s[j]);
    }




    return 0;
}
