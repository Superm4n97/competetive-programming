#include<bits/stdc++.h>
#define ll long long int
#define show(x) cout << #x << " : " << x << endl
#define scl(x) scanf("%I64d",&x)
#define maxn 2000006

using namespace std;

int main()
{
      ll tc;
      scl(tc);
      while(tc--){
            ll n;
            scl(n);

            vector<ll> v;

            for (int i=1 ; i<=2*n ; i++){
                  ll a;
                  scl(a);
                  v.push_back(a);
            }

            sort(v.begin(),v.end());

            for (int i=0 ; i<n ; i++){

            }
      }


}
